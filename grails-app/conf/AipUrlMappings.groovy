class AipUrlMappings {
    //url mapping for aip
    //UrlMappings.groovy from any plugins will be ignored to avoid conflict
    static mappings = {
//        "/ssb/aip/$action" (controller: "aip", action:$action)
    }
}

