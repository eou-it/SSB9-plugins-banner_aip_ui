class CsrUrlMappings {
    //url mapping for csr
    //UrlMappings.groovy from any plugins will be ignored to avoid conflict
    static mappings = {
//        "/ssb/csr/$action" (controller: "csr", action:$action)
    }
}

